services:
  - type: web
    name: naruto-quotes-server
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --config gunicorn.conf.py app:NarutoApi
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.19